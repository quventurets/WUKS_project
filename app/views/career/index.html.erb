<% content_for :title do %>
就職 | WUKS
<% end %>

<section class="py-5 bg-image-full" style="background-image: url(<%= asset_path('img/profile_city.jpg') %>);">
  <div style="height: 150px;">
    <div class="pt-5">
    </div>
    <div class="container">
      <div class="row">
        <div class="col-md-8 ml-auto mr-auto">
          <div class="brand">
            <h1>企業情報一覧</h1>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!--
<div class="container">
  <div class="pt-5"></div>
  <% @comps.each do |comps| %>
  <%= link_to career_show_path(name:comps.name) do %>
  <div class="row product border">
    <div class="col-md-4">
      <img src="assets/img/comp/<%= comps.name %>.jpg" alt="" class="img-thumbnail">
    </div>
    <div class="col-md-8">
      <h2 class="nameCom"><%= comps.name %></h2>
      <h4 class="forbadge">
        <span class="badge badge-pill badge-rose"><%= comps.place %></span>
      </h4>
      <h4 class="forbadge">
        <span class="badge badge-pill badge-success"><%= comps.area %></span>
        <span class="badge badge-pill badge-info"><%= comps.scale %></span>
      </h4>
    </div>
  </div>
  <% end %>
  <% end %>
</div>
-->

 <div class="container">
  <div class="row">
    <% @comps.each do |comps| %>
      <div class="col-md-3 col-sm-6" style="margin-bottom: 15px;">
        <%= link_to career_show_path(name:comps.name) do %>
          <div class="card" id="career" style="height: 90%; margin-bottom: 0px;">
            <div class="listImg">
              <img src=<%= asset_path("img/comp/#{comps.name}.jpg") %> alt="">
            </div>
            <div class="listContent">
              <div class="listTitle">
                <%= comps.name %>
              </div>
              <div class="contentDetail">
              </div>
              <div class="tagArea">
                <span class="badge badge-pill badge-rose"><%= comps.place %></span>
                <% 5.times do |i| %>
                  <% if comps.area[i] == 1 %>
                    <span class="badge badge-pill badge-success"><%= @areas[i] %></span>
                  <% end %>
                <% end %>
                <span class="badge badge-pill badge-info"><%= comps.scale %></span>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    <% end %>   

  </div>
 </div>