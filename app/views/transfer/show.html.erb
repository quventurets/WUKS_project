<% content_for :title do %>
<%= @univ.name %> | 進学
<% end %>

<section class="py-5 bg-image-full" style="background-image: url(<%= asset_path("img/univ/#{@univ.name}.jpg") %>);">
  <div style="height: 150px;">
    <div class="pt-5">
    </div>
    <div class="container">
      <div class="row">
        <div class="col-md-8 ml-auto mr-auto">
          <div class="brand">
            <h1><%= @univ.name %></h1>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<h2 class="heading">募集学部</h2>
<div class="row">
  <div class="container">
    <div class="card-header">
      <!-- Card Start -->
      <div class="card card-nav-tabs card-plain">
        <!-- Card Headder -->
        <div class="card-header card-header-primary">
          <div class="nav-tabs-navigation">
            <div class="nav-tabs-wrapper">
              <ul class="nav nav-tabs" data-tabs="tabs">
                <% @count = 0 %>
                <% @faculties.each do |faculty| %>
                <li class="nav-item">
                  <% if @count == 0%>
                  <a class="nav-link active" href="#<%= faculty.f_name %>"
                    data-toggle="tab"><%= faculty.f_name %></a>
                  <% else %>
                  <a class="nav-link" href="#<%= faculty.f_name %>" data-toggle="tab"><%= faculty.f_name %></a>
                  <% end %>
                </li>
                <% @count = @count + 1 %>
                <% end %>
              </ul>
            </div>
          </div>
        </div>
        <!-- Card Headder End -->

        <!-- Card Main Start -->
        <div class="card-body">
          <div class="tab-content univ-padding">
            <% @count = 0 %>
            <% @faculties.each do |faculty| %>
            <% if @count == 0 %>
            <div class="tab-pane active" id="<%= faculty.f_name %>">
              <% if faculty.note == "R2" %>
                <p>※R3年度編入学の募集要項がまだ出ていないため、R2年度の情報を掲載しています。</p>
              <% end %>
              <h3 class="sub">試験時期</h3>
              <ul>
                <% if faculty.is_there_recomend %>
                <li>　推薦入試：<%= faculty.date_recomend %></li>
                <% end %>
                <% if faculty.is_there_general %>
                <li>　一般入試：<%= faculty.date_general %></li>
                <% end %>
              </ul>
              <h3 class="sub">合格発表</h3>
              <ul>
                <% if faculty.is_there_recomend %>
                <li>　推薦入試：<%= faculty.date_recomend_pass %></li>
                <% end %>
                <% if faculty.is_there_general %>
                <li>　一般入試：<%= faculty.date_general_pass %></li>
                <% end %>
              </ul>
              <h3 class="sub">募集学科</h3>
              <% array_department = faculty.department.split(",") %>
              <ul class ="dep_L">
                <% array_department.each do |department| %>
                <li><%= department %></li>
                <% end %>
              </ul>
              <div class="text-center">
                <a target="_blank" href= "<%= faculty.url %>">
                  <button class="btn btn-primary">もっと詳しく見る
                    <div class="ripple-container"></div>
                  </button>
                </a>
              </div>
            </div>
            <% else %>
            <div class="tab-pane" id="<%= faculty.f_name %>">
              <% if faculty.note == "R2" %>
                <p>※R3年度編入学の募集要項がまだ出ていないため、R2年度の情報を掲載しています。</p>
              <% end %>
              <h3 class="sub">試験時期</h3>
              <ul>
                <% if faculty.is_there_recomend %>
                <li>　推薦入試：<%= faculty.date_recomend %> </li>
                <% end %>
                <% if faculty.is_there_general %>
                <li>　一般入試：<%= faculty.date_general %></li>
                <% end %>
              </ul>
              <h3 class="sub">合格発表</h3>
              <ul class>
                <% if faculty.is_there_recomend %>
                <li>　推薦入試：<%= faculty.date_recomend_pass %></li>
                <% end %>
                <% if faculty.is_there_general %>
                <li>　一般入試：<%= faculty.date_general_pass %></li>
                <% end %>
              </ul>
              <h3 class="sub">募集学科</h3>
              <% array_department = faculty.department.split(",") %>
              <ul class = "dep_L">
                <% array_department.each do |department| %>
                <li><%= department %></li>
                <% end %>
              </ul>
              <div class="text-center">
                <a href= "<%= faculty.url %>">
                  <button class="btn btn-primary">もっと詳しく見る
                    <div class="ripple-container"></div>
                  </button>
                </a>
              </div>
            </div>
            <% end %>
            <% @count = @count + 1 %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
</div>
<div class = "padding-res"></div>