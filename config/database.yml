# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  adapter: sqlite3
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000
 
development:
  <<: *default
  database: db/development.sqlite3
 
# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: db/test.sqlite3
 
production:
  adapter: mysql2
  encoding: utf8
  database: <%= ENV['MYSQLCONNSTR_DATABASE'] || 'enm_wuks' %>
  pool: 5
  username: <%= ENV['MYSQLCONNSTR_USERNAME'] || 'enm_wuks' %>
  password: <%= ENV['MYSQLCONNSTR_PASSWORD'] || 'Ietora1147' %>
  host: <%= ENV['MYSQLCONNSTR_HOST'] || 'localhost' %> 
  port: 3306
  sslca: Rails.root.join('BaltimoreCyberTrustRoot.crt.pem')
  sslverify: false
  sslcipher: AES256-SHA