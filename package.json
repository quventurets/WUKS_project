{
  "name": "wuks_project",
  "private": true,
  "scripts": {
    "build": "webpack",
    "watch": "webpack -w",
    "start": "rails server",
    "start:prd": "/bin/bash -c 'source .env.prd && rails server -e production'",
    "assets:precompile:prd": "bundle exec rake --trace assets:precompile",
    "db:migrate:prd": "/bin/bash -c 'source .env.prd && bundle exec rake db:migrate RAILS_ENV=production'",
    "db:create:prd": "/bin/bash -c 'source .env.prd && bundle exec rake db:create RAILS_ENV=production'"
  },
  "dependencies": {
    "@material-ui/core": "^4.0.2",
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  },
  "devDependencies": {
    "@babel/core": "^7.4.5",
    "@babel/polyfill": "^7.4.4",
    "@babel/preset-env": "^7.4.5",
    "@babel/preset-react": "^7.0.0",
    "babel-loader": "^8.0.6",
    "webpack": "^4.33.0",
    "webpack-cli": "^3.3.2"
  }
}
